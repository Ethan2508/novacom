import client from "./client";
import page from "./page";
import heroSettings from "./heroSettings";

/**
 * Schémas Sanity — Point d'entrée
 * Exporter tous les schémas pour la configuration Sanity
 */
export const schemaTypes = [client, page, heroSettings];
